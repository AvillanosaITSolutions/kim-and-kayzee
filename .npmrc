# Don't re-run `pnpm install` before every `pnpm run` script. Avoids a
# non-zero exit from the harmless @nestjs/core postinstall (a funding banner)
# blocking builds. esbuild's binary is approved via onlyBuiltDependencies.
verify-deps-before-run=false
